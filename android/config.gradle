ext {
    android = [
            applicationId: "com.loan.credito.cash.credit.efectivo.profin",
            compileSdk   : 33,
            minSdk       : 21,
            targetSdk    : 33,
            versionCode  : 1,
            versionName  : "1.0.0"
    ]
    dependVersion = [
            multidexVersion: '2.0.1'
    ]

    depsLibs = [
            multidex: "androidx.multidex:multidex:${dependVersion["multidexVersion"]}",
    ]
}